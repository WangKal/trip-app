{
  "builds": [
    {
      "src": "client",
      "use": "@vercel/vite",
      "dest": "dist/public",
      "installCommand": "npm install --legacy-peer-deps"
    },
    {
      "src": "server/index.ts",
      "use": "@vercel/node",
      "dest": "dist",
      "installCommand": "npm install --legacy-peer-deps"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/dist/public/$1" }
  ]
}