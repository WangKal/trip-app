{
  "builds": [
    {
      "src": "client",
      "use": "@vercel/vite",
      "installCommand": "npm install --legacy-peer-deps"
    },
    {
      "src": "server/index.ts",
      "use": "@vercel/node",
      "installCommand": "npm install --legacy-peer-deps",
      "config": {
        "outputFile": "dist/index.js"
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/dist/public/$1" }
  ]
}